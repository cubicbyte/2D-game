import Block from '../Block.js'

export default class Leaves extends Block {
    constructor() {
        super({
            properties: {
                name: 'Leaves'
            }
        })
    }
}